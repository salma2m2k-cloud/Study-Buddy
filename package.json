{
  "name": "study-buddy-backend",
  "version": "1.0.0",
  "description": "Optional backend for Study Buddy — keeps the AI API key off the frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
